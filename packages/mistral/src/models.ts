// UNVERIFIED — written without a live API key (none was available at
// implementation time). Confirm against Mistral's real model list the
// first time a MISTRAL_API_KEY is added — same "verify before trusting
// docs" discipline used for every other provider in this catalog.
export const DEFAULT_MODEL = "mistral-small-latest";

export const MODELS = [
  "mistral-small-latest",
  "mistral-large-latest",
];
